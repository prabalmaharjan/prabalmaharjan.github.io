# ----------------------------------------------------------------------------
# SITE CONFIGURATION
# ----------------------------------------------------------------------------

title: "Prabal Singh M."
description: "Programmed DNA Elimination — research notes"

# No 'theme:' line — this site uses fully custom layouts in _layouts/
# and its own stylesheet in assets/css/style.css, so no theme is needed.

plugins:
  - jekyll-feed
